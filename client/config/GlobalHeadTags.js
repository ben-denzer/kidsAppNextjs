import React from 'react';

export const Viewport = () => (
  <meta name="viewport" content="width=device-width, initial-scale=1" />
);

export const BodyMargin = () => <style>{`body { margin: 0 }`}</style>;
