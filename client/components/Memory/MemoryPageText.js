import React from 'react';

const MemoryPageText = () => (
  <React.Fragment>
    <h1>Memory Matching Game</h1>
    <h2>Remember Where To Find Each Word</h2>
    <p>
      Practice reading sight words and work on memory skills at the same time.
      Flip two cards over. If they match, then they get taken off the board. But
      if the words don't match, remember the position of the word because it
      will be useful later in the game.
    </p>
  </React.Fragment>
);

export default MemoryPageText;
