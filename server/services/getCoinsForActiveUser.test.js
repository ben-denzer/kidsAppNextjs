const chai = require('chai');
const sinon = require('sinon');
const getCoinsForActiveUser = require('./getCoinsForActiveUser');
const expect = chai.expect;

describe('getCoinsForActiveUser', () => {
  it('NEEDS TESTS');
});
