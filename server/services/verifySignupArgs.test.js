/* eslint-disable */

const chai = require('chai');
const sinon = require('sinon');
const verifySignupArgs = require('./verifySignupArgs');
const expect = chai.expect;

describe('verifySignupArgs', () => {
  it('Should Have Tests');
});
