const chai = require('chai');
const sinon = require('sinon');
const userController = require('./userController');
const expect = chai.expect;

describe('ResetPw Blackbox', () => {
  it('NEEDS TESTS');
});
